import s from './Main.module.css';
import Preloader from "../common/Preloader/Preloader";

const Main = (props) => {
    return (
        <div>
            <Preloader />
        </div>
    );
}

export default Main;
